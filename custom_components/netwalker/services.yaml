refresh:
  name: Refresh topology
  description: Trigger immediate SNMP refresh for one NetWalker entry or all entries.
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry ID to refresh. Leave empty to refresh all NetWalker entries.
      example: 0123456789abcdef0123456789abcdef
      selector:
        text:
